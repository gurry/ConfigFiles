alias em="emacs -nw"
alias ge=gedit
alias ru=ruco
alias m=more
alias tarx="tar -xvzf"
alias rmrf="rm -rf"

# These are for rustc development
alias xlf="time ./x build"
alias xl="time ./x build library --keep-stage 1"
alias xll="time ./x build library"
alias xc="time ./x check library"
alias r="time rustc +stage1"
